[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA fallback: any route not matching a static file goes to index.html,
# so client-side scroll anchors and refreshes on / don't 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200